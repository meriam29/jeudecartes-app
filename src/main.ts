import { bootstrapApplication } from '@angular/platform-browser';
import { CardGameComponent } from './app/components/card-game/card-game.component';

bootstrapApplication(CardGameComponent).catch((err) => console.error(err));
